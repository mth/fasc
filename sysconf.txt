Configuration tasks.

packages | sources.list, *apt.conf, *preferences
         | *marking libs etc as automatically installed
         | *removing unneeded packages
         | ?adding useful packages - some default package lists
         | *systemd-cron
         | *firmware
net | systemd-networkd | *wlan
                       | eth
                       | bridge
		       | vlan
    | resolv.conf, dnsmasq
    | wvdial
    | *nftables
    | simple static lan configuration using ifupdown?
*service to set pci device power to auto at boot
*grub boot delay
zram
*zswap
*sysctl | ipv6 tempaddr
        | fq_codel, westwood
        | dmesg
        | vm
        | forwarding
idcard
*hdparm
- automatic fsck - not possible anymore with systemd?
*unattended-upgrades (and automatic apt-cache updating)
??admin user
locales
pam optimization
*alsa
cvpn
rsyslog, journal
*systemd | timeouts
         | logind - suspend timeouts, sessions
         | timesyncd
? should timesyncd configured to have FallbackNTP=time.windows.com
*initramfs
nspawn
*fstab | *nfs, *usb vfat, *tmp
       | /dev/mmcblk1p1  /mnt/sd auto    noauto,user
*sudoers
sshd
?cpufreq, fancontrol
systemd hardening
hosts
modules
java, gradle
*sandboxed zoom
scripts? screen wrapper, *apt-why, *apt-upgrade

User configuration tasks
  */etc/groups
  *run-wayland, sway
  vim
  *firefox
    ff2mpv
  move ~/.cache/* into /tmp/.$user-cache/ and replace ~/.cache with symlink

profiles
 | desktop
 | java
 | laptop
 | server?

-----
Alpine setup?

-----
https://aur.archlinux.org/packages/widevine-aarch64
